{
  "name": "{{PROJECT_NAME}}-docs",
  "private": true,
  "version": "0.0.0",
  "description": "ProDocStore docs site. Static HTML + Pagefind search.",
  "scripts": {
    "prebuild": "node scripts/add-heading-ids.mjs *.html",
    "build": "pagefind --site .",
    "serve": "pagefind --site . --serve"
  },
  "devDependencies": {
    "pagefind": "^1.1.1"
  }
}
