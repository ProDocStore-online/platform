{
  "name": "freedocstore-extension",
  "private": true,
  "version": "0.47.3",
  "description": "FreeDocStore browser extension - AI-first editing for GitHub-backed public knowledge bases.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "deadcode": "knip",
    "package": "node scripts/package.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.24.0",
    "knip": "^5.88.1",
    "typescript": "^5.6.0"
  }
}
